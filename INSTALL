Simple Install Procedure
========================

  $ ./configure
  $ make
  [ Become root if necessary ]
  $ make install

Requirements
============

GLib >= 2.18
Libsoup >= 2.24
Libxml >= 2.6

Optional:

Pygobject >= 2.15.2 (for Python bindings)
Libtorrent-rasterbar >= 0.14 (for bittorrent downloads)
